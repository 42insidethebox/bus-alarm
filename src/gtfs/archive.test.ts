import{describe,expect,it}from'vitest';import{strToU8,zipSync}from'fflate';import{extractGtfsZip,GtfsArchiveError}from'./archive';
describe('GTFS ZIP extraction',()=>{it('extracts allowlisted text files from a containing folder',()=>{const zip=zipSync({'feed/stops.txt':strToU8('stop_id,stop_name\n1,Central'),'feed/routes.txt':strToU8('route_id,route_short_name\nr,42'),'readme.md':strToU8('ignore')});expect(extractGtfsZip(zip)).toEqual({'stops.txt':'stop_id,stop_name\n1,Central','routes.txt':'route_id,route_short_name\nr,42'})});it('rejects an empty archive input',()=>expect(()=>extractGtfsZip(new Uint8Array())).toThrow(GtfsArchiveError));it('rejects unsafe paths',()=>{const zip=zipSync({'../stops.txt':strToU8('x')});expect(()=>extractGtfsZip(zip)).toThrow(/unsafe file path/) });it('enforces expanded byte limits before returning files',()=>{const zip=zipSync({'stops.txt':strToU8('x'.repeat(1000))});expect(()=>extractGtfsZip(zip,{maxExpandedBytes:100})).toThrow(/too large/)})});
